# Copyright (c) 2026 Mustafa Uzumeri. All rights reserved.
#
# This file is auto-generated by reference_metadata_schema.py. DO NOT EDIT MANUALLY.
# Vertical: manufacturing
# It defines the tag vocabulary for the Cosolvent `reference_library` table
# and is part of the Knowledge Slot vertical configuration.

schema_version: '1.0'
reference_metadata_schema:
  fields:
    doc_type:
      type: enum
      allowed_values:
      - specification
      - standard
      - datasheet
      - test_method
      - drawing
      - regulation
      - guide
      - report
      - manual
      - certificate
      - other
      description: The type of the reference document.
    standard_body:
      type: enum
      allowed_values:
      - ISO
      - ASTM
      - DIN
      - EN
      - JIS
      - SAE
      - ANSI
      - MIL
      description: The standards body or framework this document belongs to (ISO /
        ASTM / DIN / ...).
    topic:
      type: enum
      allowed_values:
      - chemical_composition
      - mechanical_properties
      - dimensional_tolerance
      - surface_finish
      - heat_treatment
      - testing_inspection
      - marking_certification
      - packaging_handling
      - storage_shelf_life
      - process_requirements
      - safety_handling
      - general
      description: Specific topic or clause theme. Used to semantically tag chunks.
    material_class:
      type: enum
      allowed_values:
      - aluminum_alloy
      - carbon_steel
      - stainless_steel
      - titanium_alloy
      - nickel_alloy
      - copper_alloy
      - polymer
      - composite
      - ceramic
      - other
      description: Primary material class the document specifies.
    export_control_regime:
      type: enum
      allowed_values:
      - CGP
      - EIPA
      - none
      description: >
        Controlled-goods / export regime governing the material or process, if any.
        Canada-first: CGP (Controlled Goods Program) and EIPA (Export and Import
        Permits Act) replace the US ITAR/EAR framing.
    process_type:
      type: array
      description: Manufacturing processes the document covers (machining, casting,
        forging, additive, welding, heat_treatment, coating).
      item_type: string
    jurisdiction:
      type: array
      description: Countries or regions the document applies to.
      item_type: string
